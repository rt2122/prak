#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int
main(void)
{
    printf("#");
    //int *mem = malloc(sizeof(int) * 10);
    //mem[0] = 12;
    //scanf("%d", mem);
    //free(mem);
    _exit(0);
}
